type ProductType = {
    id: string,
    categoryId: string,
    name: string,
    description: string,
    imageUrl: string,
    priceCent: number,
    isActive: boolean
};

export default ProductType;